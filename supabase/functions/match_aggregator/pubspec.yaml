name: match_aggregator
description: Edge function to aggregate match data. 
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0"

dependencies:
  edge: ^0.0.5
  edge_http_client: ^0.0.1+3
  supabase: ^1.11.3
  supabase_functions: ^0.0.1

dev_dependencies:
  lints: ^2.0.1